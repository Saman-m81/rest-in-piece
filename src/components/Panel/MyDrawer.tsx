import { StyleSheet, Text, View } from "react-native";
import React, { FC } from "react";
import CustomeText from "../common/CustomeText";

const MyDrawer: FC = () => {
  return (
    <View>
      <CustomeText>Drawer</CustomeText>
    </View>
  );
};

export default MyDrawer;

const styles = StyleSheet.create({});
